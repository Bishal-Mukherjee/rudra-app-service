# Self-hosted Redis — 128 MB cache instance for aqua-observer-be
maxmemory 128mb
maxmemory-policy allkeys-lru

# Cache-only workload; skip persistence to reduce memory and I/O overhead.
save ""
appendonly no
